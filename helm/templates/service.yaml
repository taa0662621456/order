apiVersion: v1
kind: Service
metadata:
  name: {{ include "order-api-canary.fullname" . }}
spec:
  selector:
    app: {{ include "order-api-canary.name" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
