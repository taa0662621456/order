apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: ordercomponent-servicemonitor
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      app: ordercomponent-app
  endpoints:
    - port: http
      interval: 15s
